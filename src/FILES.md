# Frontend Source Files

Angular source entry points and global styling.

## Files

- `index.html` - Main HTML shell loaded by the browser.
- `main.ts` - Bootstraps the Angular application.
- `styles.css` - Global styles, theme variables, and shared CSS.

## Folders

- `app/` - Application components, pages, models, routes, and services.
